services:
  db:
    image: mysql:8
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      ADMIN_USERNAME: ${ADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      SCRAPE_INTERVAL_MINUTES: ${SCRAPE_INTERVAL_MINUTES}
      SCRAPE_MAX_PAGES: ${SCRAPE_MAX_PAGES:-20}
      SCRAPE_TARGET_MAX_PAGES: ${SCRAPE_TARGET_MAX_PAGES:-6}
      SCRAPE_FALLBACK_MAX_PAGES: ${SCRAPE_FALLBACK_MAX_PAGES:-20}
      MAX_CONCURRENT_SCRAPES: ${MAX_CONCURRENT_SCRAPES:-1}
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend

  bot:
    build:
      context: ./bot
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      BACKEND_API_BASE_URL: ${BACKEND_API_BASE_URL:-http://backend:8000}
      BOT_FRESH_WAIT_SECONDS: ${BOT_FRESH_WAIT_SECONDS:-12}
      BOT_STATUS_POLL_INTERVAL_SECONDS: ${BOT_STATUS_POLL_INTERVAL_SECONDS:-2}
    depends_on:
      db:
        condition: service_healthy
      backend:
        condition: service_started

volumes:
  mysql_data:
